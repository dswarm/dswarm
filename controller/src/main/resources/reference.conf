dswarm {
  # definitions related to swagger
  api {
    # the version for our API
    version = "1.0.1"
  }

  # settings for the embedded web server
  http {
    host = "127.0.0.1"
    port = 8087
    context-path = "/dmp"
  }
}

